<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.hangman">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/hangmaniconfinal"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/hangmaniconfinal"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HowToPlay"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog.Alert"></activity>
        <activity
            android:name=".LostGame"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog.Alert" />
        <activity
            android:name=".WinGame"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog.Alert" />
        <activity android:name=".PlayGame" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>